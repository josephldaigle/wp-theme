#################################
# Create server swap file
#################################
#
# creates a swap file on the server
#
commands:
    00_create_swap:
        command: bash deploy_swap_file.sh
        test: "[ free | awk '/^Swap:/ {exit !$2}' ]"
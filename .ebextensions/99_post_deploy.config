container_commands:
    configure_db:
        command: mysql -e 'CREATE DATABASE IF NOT EXISTS ebdb; GRANT ALL ON ebdb.* TO `joedaigle`@`localhost`; FLUSH PRIVILEGES;'
    export_wp_cli:
        command: "sudo bash scripts/deploy_wordpress.sh"